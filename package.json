{
  "name": "jsontodoc",
  "version": "1.0.0",
  "description": "generate word document from json file.",
  "main": "json-parse.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pruthvi menpara",
  "license": "ISC",
  "dependencies": {
    "officegen": "^0.4.0"
  }
}
